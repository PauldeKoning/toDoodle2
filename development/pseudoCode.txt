│
├── model/
│  └── Todo.js
        export default todoItem = class {
            constructor(all variables){
                //find a way to get the value of the object keys
                id = id++
                this.id = id
                this.title = title
                this.finished = finished
                this.importance = importance
                this.urgency = urgency
            }
            set title(){
                return this.title
            }

            get title(){
                return this.title
            }

            static create(todoDataObj){
            let newTodo = new Todo(todoDataObj)
            }

            static id = 0
        }


}
│  └── todoStorage.js
        export default LIST = []
│
├── controller/
│  └── todoController.js
        selectTodos(requirementsObj)
        createTodo()
            > let newTodo = >Todo.create(todoDataObj)
            > console.log(newTodo)
        removeTodo(todoId)


│  └── eventHandlers.js
        create todo with button()
            >ui.todoInput.validate()
                // />LET todoDataObj = ui.todoInput.get()

                const todoDataObj = {
                    title = "todoTitle1"
                }

                />todoController.createTodo(todoDataObj)
                />ui.displayTodoList(parentNode, todoArray)

        remove todo with button()
            if (confirm("are you sure you want to remove this todo")){
                />todoController.removeTodo(todoId)
            }
│
├── view/
│  └── domElements.js
        object with every DOM element {}
│  └── ui.js
        todoInput{}
            >validate()
                IF all inputs have correct inputs
                    />RETURN TRUE
            >get()
                RETURN all input values in an object with key.value pairs
        displayTodoDetails(parentNode, todoObj)

        displayTodoList(parentNode, todoArray)
        
│
├── main.js
├── styles.css
├── index.html
